import React, {useState} from 'react';
import {View, Text, StyleSheet} from 'react-native';

const CountryFiltre = () => {
  return (
    <View style={styles.container}>
      <Text>CountryFiltre</Text>
    </View>
  );
};

export default CountryFiltre;

const styles = StyleSheet.create({});
